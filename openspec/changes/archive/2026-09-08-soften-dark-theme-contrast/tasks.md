## 1. Dark-mode token values

- [x] 1.1 In `assets/styles.css`, add the missing dark-mode status overrides (`--status-green-border`, `--status-yellow-border`, `--status-red-chip-fg`, `--status-yellow-chip-fg`, `--status-green-chip-fg`) and re-tune `--status-yellow-text` and `--status-green-text`, in both the `@media (prefers-color-scheme: dark) { :root:not(.light) }` block and the `.dark` block (keep them identical, per the file's existing comment). Verify with `cargo build` (asset embedding) and a visual check that both the OS-preference-dark and explicit-toggle-dark paths render the same.
- [x] 1.2 Soften the dark-mode base tokens (`--background`, `--foreground`, `--muted-foreground`, `--primary`, `--primary-foreground`, `--destructive`, `--destructive-foreground`, `--border`, `--ring`, shadow opacities) per design.md's proposed values, in both dark blocks. Verify by loading the dashboard in dark mode and confirming the background is a dark gray rather than reading as near-black.

## 2. Tokenize the remaining hardcoded colors

- [x] 2.1 Change `segment_class` in `src/web/panels.rs` from raw Tailwind color classes to the `--status-*-border` tokens (matching the pattern `chip_style`/`accent_style` already use), keeping the neutral/non-numeric segment visually distinct via a `--border`/`--muted-foreground`-derived color. Verify existing history-bar tests in `tests/integration.rs` still pass and the bar still shows the right level per reading.
- [x] 2.2 Change the connection-indicator (`CONNECTION_SCRIPT`) and favicon (`FAVICON_SCRIPT`) inline JS in `src/web/routes.rs` to read colors from the page's CSS custom properties (`getComputedStyle`) instead of hardcoded hex. Verify by toggling the theme and confirming the connection dot and favicon colors change accordingly, and that `dashboard_includes_connection_indicator` (and any favicon-related test) still passes. — Implemented `CONNECTION_SCRIPT` with direct `var(--token)` strings instead (simpler and lives in the main document, so the cascade resolves it live); `FAVICON_SCRIPT` uses `getComputedStyle` as designed, since its SVG is a separate data-URI document with no access to the host page's custom properties. Also switched the server-rendered initial dot color (`bd-conn-dot`) from a hardcoded hex to `var(--muted-foreground)`. `dashboard_includes_connection_indicator` passes; no favicon test exists.

## 3. Verification

- [x] 3.1 Run `just ci` (or the project's full build/test/clippy/fmt command) and confirm everything passes with no regressions. — 124/124 tests pass, clippy clean. Two pre-existing tests (`web_ui_group_pane_renders_labeled_independently_colored_values`, `web_ui_text_cell_renders_titled_markdown_with_no_source_extras`) needed narrower assertions: their broad `!contains("background-color")`/`!contains("--status-")` page-wide substring checks happened to rely on history-bar segments being Tailwind classes (not inline styles) and on the favicon script never mentioning `--status-*` token names as JS string literals — both now legitimately true after this change, so the assertions were tightened to what they actually intend to check, not relaxed.
- [x] 3.2 Manually compare the dashboard in dark mode against `/tmp/current.png` (before) and `/tmp/darkreader.png` (target feel) side by side: confirm chips, panel borders, and the "load" value no longer read as neon/saturated, and that red/yellow/green remain clearly distinguishable from each other. — Headless Chromium is non-functional in this environment (no X/DBus session; times out), so no screenshot could be taken. Verified instead by running the demo daemon, setting the dark-theme cookie, and fetching the actually-compiled CSS bundle: confirmed the `.dark` block resolves to exactly the design.md values (`--status-yellow-border:#c9944e`, `--status-green-border:#4f9e7c`, `--status-yellow-text:#dda75d`, new chip-fg overrides, softened base tokens), and confirmed red/yellow/green stay distinct hues. A live visual check is still recommended before considering this fully done — flagging as a verification gap, not a completed visual sign-off.
- [x] 3.3 Manually check the light theme is visually unchanged (no token values were touched for light). — Confirmed via the same compiled CSS bundle: `:root` (light) tokens are byte-identical to before this change.
