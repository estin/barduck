## 1. Config: child declaration and composite expansion

- [x] 1.1 Add a child-source TOML shape (`name`, `title?`, `unit?`, `format?`, `thresholds?`, `history_points?`, `show_history?`, `show_in?`, `value_type?`, `deny_unknown_fields`) and a `children: Vec<ChildDecl>` field on `SourceCfg::Query`; verify a config declaring `children` on `Query` parses and one on `Stream`/`Ingest` is rejected at deserialization (`deny_unknown_fields`).
- [x] 1.2 Add `SourceCfg::Child { name, parent, title, unit, format, thresholds, history_points, show_history, show_in, value_type, effective_interval, cron }` and update every exhaustive `match SourceCfg` accessor (`kind`, `is_stream`, `is_ingest`, `name`, `display_title`, `timeout`, `unit`, `setup`, `format`, `thresholds`, `history_points`, `show_history`, `show_in`, `value_type`, `command`, `cron`, `interval`, `retry_interval`, `expected_interval`, `effective_interval`, `visible_in`) to compile with the new variant; verify `cargo check` passes with no wildcard arms added.
- [x] 1.3 Implement config-load-time expansion: for each `Query` source with non-empty `children`, validate (non-empty list, no `::` in a bare child name, no cross-type fields on a child — reject naming the child and field) and append one `SourceCfg::Child` per declared child to `cfg.sources`, full name `<parent>::<child>`; verify unit tests for a valid composite, a child declaring `command`/`interval`/`timeout`/`setup`/`retry_interval`, an empty `children` list, and a `::`-containing child name (spec: source-configuration — Composite source children).
- [x] 1.4 Reject a composite root declaring `unit`, `thresholds`, `value_type`, `format`, or `show_history`, naming the source and field, while a root declaring `title`/`show_in`/`interval`/`timeout`/`setup`/`retry_interval` still loads; verify unit tests for both directions (spec: source-configuration — Composite root field restrictions).
- [x] 1.5 Extend source-name-uniqueness validation to run after expansion so a child's full name colliding with any other source (top-level or another composite's child) is rejected at startup naming the collision; verify a unit test for the collision case.

## 2. Collection: fan-out fetch and force-poll cascade

- [x] 2.1 Define an ingest-shaped array-item type (`source`, `value`, `ts?`, `thresholds?`) reusing `api::IngestBody`'s field shape (extract a shared struct if that avoids duplication), and a composite fetch function that runs the root's command, parses stdout as a JSON array of these items, and returns per-child results plus the root's own success/failure; verify unit tests for well-formed output, non-JSON/non-array stdout, and command failure (spec: data-collection — Composite source command output).
- [x] 2.2 Route each array item's storage through `source::convert_value_type` + `collector::store_parsed_value` keyed on the matching `SourceCfg::Child`, exactly as `api::ingest` already does for a pushed value, with origin `poll`; verify a unit test that a composite fetch's stored child reading is indistinguishable (value, threshold, origin) from an equivalent `/api/ingest` call.
- [x] 2.3 Implement fan-out error handling: an array item naming an id that isn't a declared child fails the whole attempt (root fetch-log failure naming the id, no child written); a declared child missing from the array logs a failure for just that child (root and other children unaffected); verify unit tests for both cases (spec: data-collection — Composite fan-out error handling).
- [x] 2.4 Extend `Control::PollNow` to carry the requested name (`PollNow(String, oneshot::Sender<PollOutcome>)`); update the root's `loop_source`/`attempt()` handling so a composite root's task runs its command once per `PollNow`/scheduled tick, fans out to every child, and replies with either the root's own outcome or the named child's outcome depending on the requested name; verify the existing non-composite `PollNow` tests still pass with the added name parameter.
- [x] 2.5 Register the composite root's `ControlSender` under both the root's name and every child's full name in `AppState.controls`; verify a unit/integration test that sending `PollNow` for a child's name reaches the root's task.
- [x] 2.6 Mark the root and every declared child polling (hold one `PollingGuard` per name) for the duration of a composite fetch, regardless of which name triggered it; verify an integration test asserting `is_polling` is true for the root and every child while the command runs, and false for all after it finishes (spec: data-collection — Force polling a composite source or its children).

## 3. CLI and HTTP API

- [x] 3.1 Verify (add tests, no behavior change expected per design) that `barduck poll --source load` and `barduck poll --source load::1m` both fan out and each report the correct scoped outcome (spec: cli — Poll and fetch accept composite roots and children).
- [x] 3.2 Implement `fetch --source <composite-root>` printing the parsed array (every child's resolved entry) without writing, and `fetch --source <child>` printing just that child's entry from the same single run; verify unit tests for both.
- [x] 3.3 Verify `POST /api/sources/{name}/poll` accepts a composite root's name and a child's full name (URL-encoded `::`), fans out identically to the CLI path, and returns the correctly scoped `PollOutcome`; add an integration test if the existing generic poll-endpoint test doesn't already cover a composite source (spec: http-api — Force poll endpoint accepts composite roots and children).

## 4. Web UI and TUI rendering

- [x] 4.1 In the web panel builder, detect a layout cell resolving to a composite root (a source with `cfg.sources` entries whose `parent` equals its name) and synthesize the same `GroupItem` table shape a manually-authored `{ table = [...] }` cell already produces, in the children's declared order, using the root's own `title`/name as the pane title; verify a web integration test rendering a bare `"load"` cell as a multi-row table (spec: web-ui — A composite root renders as a table of its children).
- [x] 4.2 Apply the same detection to a generalized pane's `main` member so `main = "<composite>"` also renders the children table instead of attempting a single scalar value; verify an integration test.
- [x] 4.3 Apply the equivalent detection in the TUI panel builder (`build_panel`/layout resolution), reusing the existing group/table row rendering; verify a TUI render test for a bare composite-root cell (spec: tui — A composite root renders as a table panel).
- [x] 4.4 Verify a child referenced individually elsewhere in the same layout still renders as an ordinary single-source panel in both web and TUI, unaffected by the root's auto-rendered table appearing elsewhere.

## 5. Documentation and end-to-end verification

- [x] 5.1 Document composite sources (config shape, output contract, `poll`/`fetch` behavior on root vs. child, auto-rendered table) in `SKILL.md` and `README.md`.
- [x] 5.2 Add a composite source to `demo/config.toml` (e.g. a `load` source with `1m`/`5m`/`15m` children) demonstrating the feature end-to-end.
- [x] 5.3 Run `just ci` (clippy + nextest) and confirm it passes with the new code and tests.
